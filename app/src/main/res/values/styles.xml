<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--App Theme -->
    <style name="PizzaButlerTheme" parent="@style/Theme.AppCompat.Light" >
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryDark">@color/background_dark</item>
        <item name="colorAccent">@color/button</item>
        <item name="android:textColorPrimary">@color/text_grey</item>
        <item name="android:textColorSecondary">@color/text_red</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:buttonStyle">@style/ButtonStyle</item>
    </style>

    <!-- Button Style -->
    <style name="ButtonStyle">
        <item name="android:background">@color/button</item>
    </style>

    <!-- Button Style für Start Screen -->
    <style name="WelcomeButon">
        <item name="android:background">@color/lachs</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>